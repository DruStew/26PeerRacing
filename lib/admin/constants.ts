/** Global role rows use this scope when `scope_event_id` is not tied to a single event (see `grant_promoter_or_admin.sql`). */
export const GLOBAL_ROLE_SCOPE_ID = "00000000-0000-0000-0000-000000000000";
